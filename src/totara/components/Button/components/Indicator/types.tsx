import type { IconName } from '../../../Icon';
import type { ButtonModes } from '../../types';

export type IndicatorProps = {
  icon?: IconName;
  mode?: ButtonModes;
  color?: string;
  size?: number;
};
